# reverse-proxy: manual tests

## prepare

Create the inventory file:
````bash
cd files
cp template.broker.inventory.yml broker.inventory.yml
vi broker.inventory.yml
# add the values

````

# run

````bash
cd .devel
./run.sh
````

---
